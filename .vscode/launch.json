{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run sfml_start",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/sfml_start",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "lldb",
            "preLaunchTask": "CMake Build"
        }
    ]
}
